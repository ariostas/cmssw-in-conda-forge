# Packages that only contribute their source: detector XML, python configuration and headers.
# `cmssw-build-layer --src-only` removes the BuildFiles in their plugins/ and bin/.

# Pure python. Every cfi and cff in the release imports one of these two for the era and
# process modifiers it is customised by, so nothing in the tracking configuration can be
# imported without them.
Configuration/Eras
Configuration/ProcessModifiers

Geometry/EcalSimData
Geometry/FP420CommonData
Geometry/FP420SimData
Geometry/ForwardSimData
Geometry/HGCalSimData
Geometry/HcalSimData
Geometry/MTDSimData
Geometry/MTDTBCommonData
Geometry/MuonSimData
Geometry/TrackerSimData
Geometry/TwentyFivePercentTrackerCommonData
Geometry/VeryForwardData
Geometry/VeryForwardGeometry
Geometry/VeryForwardProtonTransport
RecoLocalTracker/Configuration
RecoMuon/Configuration
RecoTracker/Configuration
RecoTracker/IterativeTracking
RecoVertex/Configuration
TrackingTools/Configuration

# These two do have plugins, but plugins this layer cannot build. --src-only is all or
# nothing per package, so listing one here drops every plugin it has, not just the offending
# library; that is only acceptable where the package's own library is empty or the loss is
# small and clearly bounded.

# No library at all (0 translation units in src/): the layer wants their headers and python.
# NanoAOD's plugins are table producers that belong to a later, analysis-facing layer;
# DisplacedRegionalTracking's seeding producer runs a TensorFlow model.
PhysicsTools/NanoAOD
RecoTracker/DisplacedRegionalTracking

# 2 source files, 4 plugin ones. Its plugins use HLTrigger/HLTcore, which needs
# L1Trigger/L1TGlobal and so the L1 ML models (AXOL1TL, hls4ml). The dE/dx estimators come
# back with those.
RecoTracker/DeDx

# 1 source file, 4 plugin ones. Its library is what HeterogeneousCore/AlpakaCore includes;
# its plugins are the CUDA service and monitoring, which need cuda and cupti.
HeterogeneousCore/CUDAServices
