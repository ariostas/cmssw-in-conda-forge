diff --git a/SCRAM/BuildSystem/MakeInterface.py b/SCRAM/BuildSystem/MakeInterface.py
index ad56881..498b1f0 100644
--- a/SCRAM/BuildSystem/MakeInterface.py
+++ b/SCRAM/BuildSystem/MakeInterface.py
@@ -1,6 +1,13 @@
 import re
 from SCRAM import scramerror
-from os import environ, execl, sched_getaffinity
+from os import environ, execlp
+try:
+    from os import sched_getaffinity
+except ImportError:  # not available on macOS
+    from os import cpu_count
+
+    def sched_getaffinity(pid):
+        return range(cpu_count() or 1)
 
 regex_j = re.compile('^(-j|--jobs=)([0-9]*)$')
 regex_number = re.compile('^[0-9]+$')
@@ -40,6 +47,8 @@ class MakeInterface:
             environ["SCRAM_BUILDVERBOSE"] = "1"
         script = "%s/%s/SCRAM/run_gmake.sh" % (environ['LOCALTOP'], environ['SCRAM_CONFIGDIR'])
         try:
-            execl(script, *gmake_arg)
+            # Run the script with the bash found in PATH instead of its #!/bin/bash: on macOS,
+            # SIP removes DYLD_* variables (needed by the build rules) when running /bin/bash.
+            execlp("bash", "bash", script, *gmake_arg[1:])
         except Exception as e:
             scramerror("SCRAM MakeInterface::exec(): Unable to run gmake ...  %s" % e)
diff --git a/SCRAM/Core/Utils.py b/SCRAM/Core/Utils.py
index 9b86432..577f0db 100644
--- a/SCRAM/Core/Utils.py
+++ b/SCRAM/Core/Utils.py
@@ -63,6 +63,10 @@ def spawnversion(newversion='V2_99_99', basedir=None):
 def cmsos():
     e, os = SCRAM.run_command('cmsos')
     if e:
+        # cmsos (from cms-common) is not available outside of CMS installations,
+        # assume that the OS matches SCRAM_ARCH.
+        if 'SCRAM_ARCH' in environ:
+            return '_'.join(environ['SCRAM_ARCH'].split('_', 2)[:2])
         return None
     return '_'.join(os.split('_', 2)[:2])
 
